# Cambio visual desde Claude Design

Fecha:
Nombre del cambio:
Pantalla/sección afectada:
Fuente del diseño: canvas "Ceibo RRHH" en Claude Design
Carpeta en design-inbox:

## Qué quiero cambiar

Describe el cambio visual pedido.

## Qué debe preservarse

Lista ajustes previos del repo que no deben perderse.
(Siempre: shims de `build.py` y lógica de `integration/ceibo-api.js`.)

## Archivos posiblemente afectados

Lista estimada (normalmente `design/Ceibo RRHH.dc.html` por promoción;
a veces anclas de `build.py`).

## Resultado esperado

Describe cómo debería verse al final.

## Criterios de aceptación

- El cambio se ve igual o equivalente al diseño.
- No se pierden ajustes anteriores.
- No se reemplaza la app completa.
- `python build.py` corre sin errores de anclas y `dist/` funciona contra el backend.
- El diff es entendible.
